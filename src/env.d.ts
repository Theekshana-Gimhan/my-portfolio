// This file declares build-time globals injected by Vite's `define` option.
// `__BASE_PATH__` is provided from `process.env.BASE_PATH` in `vite.config.ts`.
// Declaring it here keeps TypeScript and IDEs happy while allowing the
// runtime build to replace the value during compilation.
declare const __BASE_PATH__: string;

export {};
